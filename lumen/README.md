# lumen

A from-scratch compiler for **Vita** (`.vt` files), built in Rust, targeting native code via LLVM

This is the baseline: it only understands one program shape, and is meant to be extended step by step from here

```
main {
    return 1;
}
```
