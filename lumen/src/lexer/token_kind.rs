#[derive(Debug, Clone, PartialEq)]
pub enum TokenKind {
    Return,
    Int(i64),
    LBrace,
    RBrace,
    LParen,
    RParen,
    Semicolon,
    Plus,
    Minus,
    Star,
    Slash,
    Var,
    Val,
    If,
    Elif,
    Else,
    Identifier(String),
    Equal,
}